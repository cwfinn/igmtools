[FIGURE]

filename = string
transitions = string
vmin = float
vmax = float
nrows = integer(default=None)
ncols = integer(default=None)
width = float(default=8.0)
aspect = float(default=0.3)
fontsize = float(default=16)
label_fontsize = float(default=12)
family = string(default='serif')
style = string(default='Times')
weight = string(default='normal')
usetex = boolean(default=False)
tick_major_length = float(default=7)
tick_major_width = float(default=1.2)
tick_minor_length = float(default=4)
tick_minor_width = float(default=1)

[DATA]

filename = string
wavelength_column = string
flux_column = string
error_column = string
continuum_column = string
redshift = float
colour = string(default='0.5')
linewidth = float(default=1.0)

[MODEL]

filename = string(default=None)
ion_column = string
redshift_column = string
logn_column = string
b_column = string
system_width = float
resolution = float
convolve_with_cos_fuv = boolean(default=False)
convolve_with_cos_nuv = boolean(default=False)
oversample = integer(default=1)
plot_ticks = boolean(default=True)
plot_components = boolean(default=False)
plot_residuals = boolean(default=False)
system_colour = string(default='b')
blend_colour = string(default='r')
linewidth = float(default=1.0)
component_linewidth = float(default=0.5)
tick_linewidth = float(default=1.0)
residuals_colour = string(default='g')
residuals_markersize = float(default=2.0)